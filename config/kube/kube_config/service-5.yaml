apiVersion: v1
kind: Service
metadata:
  name: board-generator-5
  namespace: board-generator
spec:
  selector:
    id: board-generator-5
  ports:
  - protocol: TCP
    port: 50052
    targetPort: 50052
