apiVersion: v1
kind: Service
metadata:
  name: board-generator-10
  namespace: board-generator
spec:
  selector:
    id: board-generator-10
  ports:
  - protocol: TCP
    port: 50052
    targetPort: 50052
