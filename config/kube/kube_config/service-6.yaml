apiVersion: v1
kind: Service
metadata:
  name: board-generator-6
  namespace: board-generator
spec:
  selector:
    id: board-generator-6
  ports:
  - protocol: TCP
    port: 50052
    targetPort: 50052
